// set SERVER_HOSTNAME to Flask app hostname
// (see SweetPea/server/Server.py)
export const SERVER_HOSTNAME = 'localhost';

// set SERVER_PORT to Flask app port
// (see SweetPea/core-server/Server.py)
export const SERVER_PORT = 5000;

// API endpoint(s)
export const TIME_ENDPOINT = "time";